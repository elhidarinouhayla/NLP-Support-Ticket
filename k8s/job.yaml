apiVersion: batch/v1
kind: Job
metadata:
  name: nlp-ticket-job
spec:
  template:
    spec:
      containers:
        - name: nlp-container
          image: nlp-ticket-pipeline:latest
          imagePullPolicy: IfNotPresent
      restartPolicy: Never
  backoffLimit: 2
